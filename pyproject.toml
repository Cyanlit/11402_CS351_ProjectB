[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "csv-mini-db"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "tabulate",
]

[tool.setuptools.packages.find]
where = ["src"]
